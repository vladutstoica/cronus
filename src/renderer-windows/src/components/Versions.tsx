// Version information not currently exposed through the preload API
function Versions(): React.JSX.Element {
  return (
    <ul className="versions">
      <li className="app-version">Cronus App</li>
    </ul>
  );
}

export default Versions;
