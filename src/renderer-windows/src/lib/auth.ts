// This file is no longer used - authentication has been removed
// Keeping as stub to avoid import errors in case of missed references

export const login = () => {
  console.warn(
    "Login is no longer available - app runs without authentication",
  );
};

export const logout = () => {
  console.warn(
    "Logout is no longer available - app runs without authentication",
  );
};
